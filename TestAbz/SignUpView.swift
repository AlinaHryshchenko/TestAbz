

import Foundation
import SwiftUI

struct SignUpView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Nunito Sans Nunito Sans")
                .font(.custom("Nunito Sans", size: 24))
        }
    }
}

#Preview {
    SignUpView()
}
